// TODO (Phase A frontend, GREEN step):
// Implement this component to satisfy src/__tests__/LoginForm.test.js:
// - render <form> with email + password <input> and a submit <button>
// - on submit, call an api/auth.login(email, password)
// - on success, store token (via AuthContext) and redirect to /dashboard
// - on failure, render an error message

export default function LoginForm() {
  return null;
}
