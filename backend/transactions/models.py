from django.db import models

# TODO (Phase C - TDD):
# Write transactions/tests/test_models.py first (Transaction: vehicle FK,
# user FK, type PURCHASE/RESTOCK, quantity, timestamp). Then implement.
