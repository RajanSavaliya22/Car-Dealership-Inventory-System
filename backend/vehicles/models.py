from django.db import models

# TODO (Phase B - TDD):
# Write vehicles/tests/test_models.py first (make, model, year, price,
# quantity, description, image). Add a check constraint or validator so
# quantity can never go negative. Then implement the model here.
